/**
 * Firebase Configuration — REMOVED
 *
 * Firebase native modules have been removed from the app.
 * Push notifications now use Expo Notifications.
 * This file is kept as a no-op to prevent import errors from any stale references.
 */
export default null;
