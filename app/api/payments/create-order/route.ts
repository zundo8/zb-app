/**
 * Alias for /api/payment/create-order — kept for backward compatibility
 * and to satisfy the paths mentioned in STEP 8 of the payment spec.
 */
export { POST, OPTIONS } from '../../payment/create-order/route';
