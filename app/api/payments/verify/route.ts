/**
 * Alias for /api/payment/verify — kept for backward compatibility
 * and to satisfy the paths mentioned in STEP 9 of the payment spec.
 */
export { POST, OPTIONS } from '../../app/payment/verify/route';
