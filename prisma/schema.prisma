generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Shop {
  id                       String     @id @default(cuid())
  domain                   String     @unique
  accessToken              String
  installedAt              DateTime   @default(now())
  updatedAt                DateTime   @updatedAt
  delhiveryApiKey          String?
  razorpayKeyId            String?
  razorpayKeySecret        String?
  shiprocketEmail          String?
  shiprocketToken          String?
  webhookSecret            String?
  heroImage                String?
  heroVideo                String?
  heroTitle                String?
  heroSubtitle             String?
  heroButtonText           String?    @default("Discover")
  latestCurationTitle      String?    @default("Latest curation")
  latestCurationSubtitle   String?    @default("Season Drop")
  archiveTitle             String?    @default("The Archive")
  archiveSubtitle          String?    @default("Organic Evolution")
  blueprintTitle           String?    @default("The blueprint of Zica Bella")
  blueprintSubtitle        String?    @default("Technique & Motion")
  showHeroText             Boolean    @default(false)
  showLatestCuration       Boolean    @default(true)
  showArchive              Boolean    @default(true)
  showBlueprint            Boolean    @default(true)
  showProductVideo         Boolean    @default(true)
  showSizeChart            Boolean    @default(true)
  showBrand                Boolean    @default(true)
  showShippingReturn       Boolean    @default(true)
  showCare                 Boolean    @default(true)
  showSizeFit              Boolean    @default(true)
  showDetails              Boolean    @default(true)
  pdpBackground            String?
  instagramUrl             String?
  appleUrl                 String?
  spotifyUrl               String?
  youtubeUrl               String?
  featuredMedia            String?
  collectionsMedia         String?
  footerVideo              String?
  mainMenuHandle           String?
  secondaryMenuHandle      String?
  showTreeText             Boolean    @default(true)
  enabledCollectionsHeader String?
  enabledCollectionsPage   String?
  enabledCollectionsMenu   String?
  featuredMediaImage       String?
  kineticMeshProducts      String?
  communitySubtitle        String?    @default("Community")
  communityTitle           String?    @default("Featured Looks")
  kineticMeshTitle         String?    @default("ARCHIVE EDITION")
  showCommunity            Boolean    @default(true)
  spotlightSubtitle        String?    @default("Luxury Indian streetwear for modern men. Redefining bold everyday style.")
  spotlightTitle           String?    @default("AUTHENTIC STREETWEAR")
  chatAccessMode           String     @default("open")
  communityAgeRestricted   Boolean    @default(true)
  communityMinOrders       Int        @default(1)
  communityWhatsAppEnabled Boolean    @default(true)
  flipbookConfig           String?    @default("[]")
  flipbookDesc             String?    @default("Engineered for those who move without compromise.")
  flipbookImage            String?
  flipbookTag              String?    @default("Core Manifest")
  flipbookTitle            String?    @default("Archival Vision")
  flipbookVideo            String?
  ringCarouselItems        String?    @default("[]")
  ringCarouselTitle        String?    @default("RING COLLECTION")
  showRingCarousel         Boolean    @default(true)
  spotlightCollection      String?    @default("tshirts")
  spotlightProducts        String?    @default("")
  shiprocketPassword       String?
  firebaseClientEmail      String?
  firebasePrivateKey       String?
  firebaseProjectId        String?
  sendgridApiKey           String?
  twilioAccountSid         String?
  twilioAuthToken          String?
  twilioPhoneNumber        String?
  whatsappPhoneId          String?
  whatsappToken            String?
  footerLogo3dUrl          String?    @default("https://cdn.shopify.com/3d/models/e024b09e83a75c03/Zicabella-silver-logo.glb")
  spotlightMedia           String?
  claudeApiKey             String?
  claudeWebhookSecret      String?
  openaiApiKey             String?
  openaiWebhookSecret      String?
  homepageCollection       String?    @default("")
  homepageProducts         String?    @default("")
  heroVideoMobile          String?
  collectionsMediaMobile   String?
  featuredMediaImageMobile String?
  featuredMediaMobile      String?
  flipbookImageMobile      String?
  flipbookVideoMobile      String?
  footerVideoMobile        String?
  loginBgImage             String?
  loginBgImageMobile       String?
  loginBgVideo             String?
  loginBgVideoMobile       String?
  loginBgImageDark         String?
  loginBgImageDarkMobile   String?
  loginBgImageLight        String?
  loginBgImageLightMobile  String?
  featuredMediaLink        String?
  flipbookLink             String?
  collectionsMediaLink     String?
  collectionProductOrders  String?    @default("{}")
  shopAllLink              String?    @default("/collections/all")
  feedExcludedCollections  String?    @default("[]")
  zicaAiSupportEnabled     Boolean    @default(true)
  zicaAiWhatsappEnabled    Boolean    @default(true)
  customers                Customer[]
  orders                   Order[]
  products                 Product[]
}

model ProductMoodBoard {
  id               String   @id @default(cuid())
  shopifyProductId String   @unique
  images           String   @default("[]")
  displayOrder     Int      @default(0)
  updatedAt        DateTime @updatedAt
  createdAt        DateTime @default(now())
}

model Customer {
  id                    String             @id @default(cuid())
  shopId                String
  shopifyId             String             @unique
  email                 String?
  name                  String?
  phone                 String?
  phoneLast10           String?
  defaultAddress        String?
  ordersCount           Int                @default(0)
  totalSpent            Float              @default(0)
  createdAt             DateTime           @default(now())
  updatedAt             DateTime           @updatedAt
  image                 String?
  region                String?
  storeCredits          Float              @default(0)
  storeCreditPreference Boolean            @default(false)
  emailOptedOut         Boolean            @default(false)
  smsOptedOut           Boolean            @default(false)
  whatsappOptedOut      Boolean            @default(false)
  lastLoginAt           DateTime?
  accountCreatedMsgSentAt DateTime?
  addresses             Address[]
  communityMember       CommunityMember?
  communityMessages     CommunityMessage[]
  shop                  Shop               @relation(fields: [shopId], references: [id])
  following             Follow[]           @relation("UserFollowing")
  followers             Follow[]           @relation("UserFollowers")
  mobileOrders          MobileOrder[]
  orders                Order[]
  payments              Payment[]
  profileHistory        ProfileHistory[]
  returns               Return[]
  storeCreditTxns       StoreCredit[]
  wishlist              Wishlist[]
  carts                 Cart[]
  supportTickets        SupportTicket[]

  @@index([phone])
  @@index([phoneLast10])
}

model Cart {
  id               String     @id @default(cuid())
  customerId       String?    @map("customer_id")
  sessionToken     String?    @unique @map("session_token")
  source           String     @default("webstore")
  status           String     @default("active")
  phone            String?    @map("phone_number")
  phoneLast10      String?    @map("phone_last_10")
  email            String?
  subtotal         Float      @default(0)
  currency         String     @default("INR")
  createdAt        DateTime   @default(now()) @map("created_at")
  updatedAt        DateTime   @updatedAt @map("updated_at")
  lastActivityAt   DateTime   @default(now()) @map("last_activity_at")
  abandonedAt      DateTime?  @map("abandoned_at")
  convertedOrderId String?    @unique @map("converted_order_id")
  city             String?
  country          String?
  latitude         Float?
  longitude        Float?
  state            String?
  zip              String?
  items            CartItem[]
  convertedOrder   Order?     @relation("ConvertedOrder", fields: [convertedOrderId], references: [id])
  customer         Customer?  @relation(fields: [customerId], references: [id], onDelete: Cascade)
  whatsAppMessages WhatsAppMessage[]

  @@index([customerId])
  @@index([status])
  @@index([lastActivityAt])
  @@index([source])
  @@index([createdAt])
  @@index([phoneLast10])
  @@index([abandonedAt])
  @@map("cart_sessions")
}

model CartItem {
  id        String   @id @default(cuid())
  cartId    String   @map("cart_session_id")
  productId String   @map("shopify_product_id")
  variantId String?  @map("shopify_variant_id")
  handle    String?
  title     String?  @map("product_title")
  price     Float?
  image     String?  @map("image_url")
  quantity  Int
  size      String?  @map("variant_title")
  createdAt DateTime @default(now()) @map("added_at")
  cart      Cart     @relation(fields: [cartId], references: [id], onDelete: Cascade)

  @@index([cartId])
  @@map("cart_items")
}

model ProfileHistory {
  id         String   @id @default(cuid())
  customerId String
  oldName    String?
  newName    String?
  oldEmail   String?
  newEmail   String?
  oldPhone   String?
  newPhone   String?
  oldImage   String?
  newImage   String?
  changedBy  String   @default("USER")
  createdAt  DateTime @default(now())
  customer   Customer @relation(fields: [customerId], references: [id], onDelete: Cascade)

  @@index([customerId])
  @@index([createdAt])
}

model Follow {
  id          String   @id @default(cuid())
  followerId  String
  followingId String
  createdAt   DateTime @default(now())
  follower    Customer @relation("UserFollowing", fields: [followerId], references: [id], onDelete: Cascade)
  following   Customer @relation("UserFollowers", fields: [followingId], references: [id], onDelete: Cascade)

  @@unique([followerId, followingId])
}

model Order {
  id                    String            @id @default(cuid())
  shopId                String
  shopifyOrderId        String?           @unique
  customerId            String
  status                String
  totalPrice            Float
  subtotalPrice         Float?
  totalTax              Float?
  currency              String            @default("INR")
  displayCountry        String?           @map("display_country")
  paymentStatus         String
  fulfillmentStatus     String
  deliveryStatus        String            @default("pending")
  shippingAddress       String?
  billingAddress        String?
  note                  String?
  createdAt             DateTime          @default(now())
  updatedAt             DateTime          @updatedAt
  orderType             String            @default("REGULAR")
  tags                  String?
  aiRiskRecommendation  String?
  codConfirmationMethod String?
  codConfirmationStatus String?
  codConfirmedAt        DateTime?
  paymentCapturedAt     DateTime?
  paymentMethod         String?
  razorpayOrderId       String?           @unique
  razorpayPaymentId     String?           @unique
  rtoRiskFactors        String?
  rtoRiskScore          Int?
  discountAmount        Float?            @default(0)
  storeCreditAmount     Float?            @default(0)
  discountCode          String?
  delhivery_awb         String?
  tracking_status       String?
  internalOrderNumber   String?           @unique @map("internal_order_number")
  shopifyOrderName      String?           @map("shopify_order_name")
  shopifySyncStatus     String            @default("synced") @map("shopify_sync_status")
  shopifySyncError      String?           @map("shopify_sync_error")
  refundStatus          String            @default("not_applicable") @map("refund_status")
  refundError           String?           @map("refund_error")
  refundAttempts        Int               @default(0) @map("refund_attempts")
  cancelledAt           DateTime?         @map("cancelled_at")
  cancelledBy           String?           @map("cancelled_by")
  paymentFailureReason  String?           @map("payment_failure_reason")
  previousOrderNumbers  String?           @map("previous_order_numbers")
  deliveredAt           DateTime?         @map("delivered_at")
  exchanges             Exchange[]
  exchangeRequests      ExchangeRequest[]
  customer              Customer          @relation(fields: [customerId], references: [id])
  shop                  Shop              @relation(fields: [shopId], references: [id])
  items                 OrderItem[]
  payments              Payment[]
  returns               Return[]
  returnRequests        ReturnRequest[]
  shipments             Shipment[]
  cartSession           Cart?             @relation("ConvertedOrder")
  webhookEvents         WebhookEvent[]

  @@index([createdAt])
  @@index([status, createdAt])
  @@index([paymentStatus, createdAt])
  @@index([orderType, createdAt])
  @@index([customerId, createdAt])
  @@index([refundStatus])
}

model OrderItem {
  id                String   @id @default(cuid())
  orderId           String
  shopifyLineItemId String   @unique
  productId         String?
  title             String
  quantity          Int
  price             Float
  sku               String?
  image             String?
  variantId         String?
  variantTitle      String?
  size              String?
  order             Order    @relation(fields: [orderId], references: [id], onDelete: Cascade)
  product           Product? @relation(fields: [productId], references: [id])
}

model Product {
  id                String            @id @default(cuid())
  shopId            String
  shopifyProductId  String            @unique
  title             String
  includeInFeed     Boolean           @default(true)
  sku               String?
  barcode           String?
  inventoryItemId   String?           @unique
  createdAt         DateTime          @default(now())
  updatedAt         DateTime          @updatedAt
  featuredImage     String?
  handle            String?
  price             Float?
  newExchanges      Exchange[]        @relation("NewProduct")
  originalExchanges Exchange[]        @relation("OriginalProduct")
  inventory         Inventory[]
  mobileOrderItems  MobileOrderItem[]
  orderItems        OrderItem[]
  shop              Shop              @relation(fields: [shopId], references: [id])
  returns           Return[]
  wishlist          Wishlist[]
}

model Inventory {
  id               String   @id @default(cuid())
  productId        String
  locationId       String
  stockQuantity    Int      @default(0)
  reservedQuantity Int      @default(0)
  updatedAt        DateTime @updatedAt
  product          Product  @relation(fields: [productId], references: [id], onDelete: Cascade)

  @@unique([productId, locationId])
}

model Return {
  id                String         @id @default(cuid())
  orderId           String
  productId         String
  customerId        String
  sku               String?
  quantity          Int            @default(1)
  reason            String
  status            String         @default("REQUESTED")
  requestedAt       DateTime       @default(now())
  updatedAt         DateTime       @updatedAt
  returnMethod      String?
  refundMethod      String?
  trackingNumber    String?
  refundAmount      Float?
  storeCreditAmount Float?
  refundStatus      String?
  returnRequestId   String?
  variantTitle      String?
  size              String?
  title             String?
  customer          Customer       @relation(fields: [customerId], references: [id])
  order             Order          @relation(fields: [orderId], references: [id], onDelete: Cascade)
  product           Product        @relation(fields: [productId], references: [id])
  returnRequest     ReturnRequest? @relation(fields: [returnRequestId], references: [id])
}

model ReturnRequest {
  id              String    @id @default(cuid())
  orderId         String
  customerId      String
  status          String    @default("pending_approval")
  estimatedRefund Float     @default(0)
  actualRefund    Float?
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
  approvedAt      DateTime?
  reason          String?
  reverseAwb      String?
  returns         Return[]
  order           Order     @relation(fields: [orderId], references: [id], onDelete: Cascade)
}

model Exchange {
  id                   String           @id @default(cuid())
  orderId              String
  originalProductId    String
  newProductId         String
  status               String           @default("REQUESTED")
  priceDifference      Float            @default(0)
  createdAt            DateTime         @default(now())
  updatedAt            DateTime         @updatedAt
  paymentStatus        String?
  newOrderId           String?
  exchangeRequestId    String?
  reason               String?
  qcStatus             String?          @default("pending")
  qcNotes              String?
  originalVariantTitle String?
  originalSize         String?
  newVariantTitle      String?
  newSize              String?
  exchangeRequest      ExchangeRequest? @relation(fields: [exchangeRequestId], references: [id])
  newProduct           Product          @relation("NewProduct", fields: [newProductId], references: [id])
  order                Order            @relation(fields: [orderId], references: [id], onDelete: Cascade)
  originalProduct      Product          @relation("OriginalProduct", fields: [originalProductId], references: [id])
}

model ExchangeRequest {
  id                   String     @id @default(cuid())
  orderId              String
  customerId           String
  status               String     @default("pending_approval")
  priceDifference      Float      @default(0)
  paymentStatus        String     @default("not_required")
  paymentId            String?
  createdAt            DateTime   @default(now())
  updatedAt            DateTime   @updatedAt
  reason               String?
  returnRequestId      String?
  newShopifyOrderId    String?
  reverseAwb           String?
  settlementPreference String     @default("PREPAID_NOW")
  exchanges            Exchange[]
  order                Order      @relation(fields: [orderId], references: [id], onDelete: Cascade)
}

model Payment {
  id           String   @id @default(cuid())
  orderId      String
  customerId   String
  amount       Float
  type         String
  status       String
  gateway      String
  currencyCode String?  @default("INR") @map("currency_code")
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  customer     Customer @relation(fields: [customerId], references: [id])
  order        Order    @relation(fields: [orderId], references: [id], onDelete: Cascade)
}

model Shipment {
  id                   String    @id @default(cuid())
  orderId              String
  trackingNumber       String?
  courier              String?
  status               String
  type                 String    @default("outbound")
  createdAt            DateTime  @default(now())
  updatedAt            DateTime  @updatedAt
  trackingUrl          String?
  currentLocation      String?
  estimatedDelivery    DateTime?
  events               String?   @default("[]")
  awb                  String?   @unique
  labelUrl             String?
  rawDelhiveryResponse String?
  order                Order     @relation(fields: [orderId], references: [id], onDelete: Cascade)
}

model WebhookEvent {
  id          String    @id @default(cuid())
  source      String
  eventType   String
  payload     String
  processed   Boolean   @default(false)
  processedAt DateTime?
  createdAt   DateTime  @default(now())
  orderId     String?
  order       Order?    @relation(fields: [orderId], references: [id])

  @@index([source, eventType])
  @@index([createdAt])
  @@index([orderId])
}

model FeaturedUser {
  id               String   @id @default(cuid())
  orderId          String?
  name             String
  email            String
  imageUrl         String
  styleDescription String?
  isTopFeatured    Boolean  @default(false)
  status           String   @default("PENDING")
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
  instagramUrl     String?
  reviews          Review[]
}

model Review {
  id             String       @id @default(cuid())
  featuredUserId String
  reviewerName   String
  rating         Int          @default(5)
  comment        String?
  status         String       @default("APPROVED")
  createdAt      DateTime     @default(now())
  featuredUser   FeaturedUser @relation(fields: [featuredUserId], references: [id], onDelete: Cascade)
}

model ProductReview {
  id               String   @id @default(cuid())
  productId        String
  orderId          String
  userId           String
  rating           Int
  title            String?
  body             String
  verifiedPurchase Boolean  @default(true)
  status           String   @default("VISIBLE")
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  @@unique([userId, productId])
  @@index([productId])
  @@index([status])
}

model Admin {
  id        String   @id @default(cuid())
  username  String   @unique
  password  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model BlogPost {
  id              String   @id @default(cuid())
  title           String
  slug            String   @unique
  excerpt         String?
  content         String
  coverImage      String?
  published       Boolean  @default(false)
  author          String   @default("Zica Bella")
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  metaTitle       String?  @map("meta_title")
  metaDescription String?  @map("meta_description")
  backlinkUrl     String?  @map("backlink_url")
  backlinkText    String?  @map("backlink_text")
  indexPref       Boolean  @default(true) @map("index_pref")
}

model CommunityMember {
  id            String    @id @default(cuid())
  customerId    String    @unique
  isVerified    Boolean   @default(false)
  status        String    @default("PENDING")
  phone         String?
  dob           DateTime?
  whatsappOptIn Boolean   @default(false)
  joinedAt      DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  customer      Customer  @relation(fields: [customerId], references: [id])
}

model CommunityUpdate {
  id          String    @id @default(cuid())
  type        String    @default("EVENT")
  title       String
  description String?
  imageUrl    String?
  date        DateTime?
  published   Boolean   @default(true)
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
}

model CommunityMessage {
  id         String         @id @default(cuid())
  customerId String
  content    String
  imageUrl   String?
  createdAt  DateTime       @default(now())
  reactions  ChatReaction[]
  customer   Customer       @relation(fields: [customerId], references: [id])
}

model ChatReaction {
  id         String           @id @default(cuid())
  messageId  String
  customerId String
  emoji      String           @default("❤️")
  createdAt  DateTime         @default(now())
  message    CommunityMessage @relation(fields: [messageId], references: [id], onDelete: Cascade)

  @@unique([messageId, customerId, emoji])
}

model ScanRecord {
  id           String   @id @default(cuid())
  productId    String?
  productTitle String?
  variantInfo  String?
  sku          String?
  barcode      String?
  actionType   String
  quantity     Int      @default(1)
  beforeStock  Int?
  afterStock   Int?
  locationId   String?
  staffName    String   @default("Admin")
  createdAt    DateTime @default(now())
}

model MfgFabric {
  id                      String               @id @default(cuid())
  sku                     String               @unique
  name                    String
  costPerMeter            Float
  weightValue             Float                @default(0)
  weightUnit              String               @default("kg")
  totalMeters             Float                @default(0)
  status                  String               @default("ACTIVE")
  lowStockMetersThreshold Float?
  createdAt               DateTime             @default(now())
  updatedAt               DateTime             @updatedAt
  movements               MfgFabricMovement[]
  productionBatches       MfgProductionBatch[]
}

model MfgFabricMovement {
  id                 String              @id @default(cuid())
  fabricId           String
  occurredAt         DateTime            @default(now())
  type               String
  quantity           Float
  quantityUnit       String
  rateAtMovement     Float
  totalValue         Float
  remarks            String?
  createdByName      String              @default("Admin")
  correctsMovementId String?
  productionBatchId  String?
  balanceMetersAfter Float?
  balanceWeightAfter Float?
  createdAt          DateTime            @default(now())
  fabric             MfgFabric           @relation(fields: [fabricId], references: [id], onDelete: Cascade)
  batch              MfgProductionBatch? @relation(fields: [productionBatchId], references: [id])

  @@index([fabricId, occurredAt])
  @@index([productionBatchId])
}

model MfgProductionBatch {
  id                   String                  @id @default(cuid())
  batchCode            String                  @unique @map("batch_code")
  productName          String                  @map("product_name")
  quantity             Int
  currentStage         String                  @map("current_stage")
  washCostTotal        Float                   @default(0) @map("wash_cost_total")
  fabricId             String?                 @map("fabric_id")
  notes                String?
  estimatedCostPerUnit Float?                  @map("estimated_cost_per_unit")
  createdAt            DateTime                @default(now()) @map("created_at")
  updatedAt            DateTime                @updatedAt @map("updated_at")
  isCuttingDone        Boolean                 @default(false) @map("is_cutting_done")
  isEmbroideryDone     Boolean                 @default(false) @map("is_embroidery_done")
  isPrintingDone       Boolean                 @default(false) @map("is_printing_done")
  isSampleDone         Boolean                 @default(false) @map("is_sample_done")
  isWashingDone        Boolean                 @default(false) @map("is_washing_done")
  isStitchingDone      Boolean                 @default(false) @map("is_stitching_done")
  workdriveFolderId    String?                 @map("workdrive_folder_id")
  workdriveUrl         String?                 @map("workdrive_url")
  workdriveFolderName  String?                 @map("workdrive_folder_name")
  batchNotes           MfgBatchNote[]
  movements            MfgFabricMovement[]
  miscExpenses         MfgMiscExpense[]
  stageLogs            MfgProductionStageLog[]
  fabric               MfgFabric?              @relation(fields: [fabricId], references: [id])
  tasks                MfgTask[]

  @@map("manufacturing_batches")
}

model MfgTask {
  id                  String              @id @default(cuid())
  title               String
  description         String?
  status              String              @default("PENDING")
  priority            String              @default("MEDIUM")
  dueDate             DateTime?           @map("due_date")
  completedAt         DateTime?           @map("completed_at")
  createdAt           DateTime            @default(now()) @map("created_at")
  updatedAt           DateTime            @updatedAt @map("updated_at")
  createdByName       String              @default("Admin") @map("created_by_name")
  batchId             String?             @map("batch_id")
  approvalStatus      String?             @map("approval_status")
  assignedToId        String?             @map("assigned_to_id")
  designImage         String?             @map("design_image")
  designName          String?             @map("design_name")
  type                String              @default("MANUAL")
  workdriveFolderId   String?             @map("workdrive_folder_id")
  workdriveUrl        String?             @map("workdrive_url")
  workdriveFolderName String?             @map("workdrive_folder_name")
  assignedTo          User?               @relation("TaskAssignee", fields: [assignedToId], references: [id])
  batch               MfgProductionBatch? @relation(fields: [batchId], references: [id])

  @@map("pending_tasks")
}

model MfgProductionStageLog {
  id            String             @id @default(cuid())
  batchId       String
  action        String
  fromStage     String?
  toStage       String?
  payload       Json
  costAmount    Float              @default(0)
  createdByName String
  createdAt     DateTime           @default(now())
  batch         MfgProductionBatch @relation(fields: [batchId], references: [id], onDelete: Cascade)

  @@index([batchId, createdAt])
}

model MfgMiscExpense {
  id            String              @id @default(cuid())
  batchId       String?
  amount        Float
  description   String
  expenseType   String              @default("OTHER")
  expenseDate   DateTime
  createdByName String
  createdAt     DateTime            @default(now())
  batch         MfgProductionBatch? @relation(fields: [batchId], references: [id], onDelete: Cascade)

  @@index([batchId])
}

model MfgBatchNote {
  id            String             @id @default(cuid())
  batchId       String
  content       String
  createdByName String
  createdAt     DateTime           @default(now())
  batch         MfgProductionBatch @relation(fields: [batchId], references: [id], onDelete: Cascade)

  @@index([batchId, createdAt])
}

model MfgAuditLog {
  id         String   @id @default(cuid())
  entityType String
  entityId   String
  action     String
  details    Json?
  actorName  String
  createdAt  DateTime @default(now())

  @@index([entityType, entityId])
  @@index([createdAt])
}

model MfgVendor {
  id            String                    @id @default(cuid())
  name          String
  address       String?
  mobile        String?
  category      String
  createdAt     DateTime                  @default(now())
  updatedAt     DateTime                  @updatedAt
  contactPerson String?                   @map("contact_person")
  email         String?
  leadTimeDays  Int?                      @map("lead_time_days")
  pricingNotes  String?                   @map("pricing_notes")
  remarks       String?
  kbEntries     MfgProductKnowledgeBase[] @relation("KBVendor")
  samples       MfgSample[]
}

model MfgDesignTask {
  id                  String    @id @default(cuid())
  title               String
  description         String?
  orderId             String?   @map("order_id")
  status              String    @default("draft")
  workdriveFolderId   String?   @map("workdrive_folder_id")
  workdriveFolderName String?   @map("workdrive_folder_name")
  approvedFileId      String?   @map("approved_file_id")
  approvedById        String?   @map("approved_by")
  approvedAt          DateTime? @map("approved_at")
  createdById         String?   @map("created_by")
  createdAt           DateTime  @default(now()) @map("created_at")
  updatedAt           DateTime  @updatedAt @map("updated_at")
  approvedBy          User?     @relation("DesignApprover", fields: [approvedById], references: [id])
  createdBy           User?     @relation("DesignCreator", fields: [createdById], references: [id])

  @@index([status])
  @@index([orderId])
  @@map("design_tasks")
}

model StoreCredit {
  id              String    @id @default(cuid())
  customerId      String
  amount          Float
  type            String
  description     String
  orderId         String?
  returnId        String?
  createdAt       DateTime  @default(now())
  expiresAt       DateTime?
  remainingAmount Float     @default(0)
  customer        Customer  @relation(fields: [customerId], references: [id], onDelete: Cascade)

  @@index([customerId, createdAt])
}

model DeviceToken {
  id         String   @id @default(cuid())
  userId     String
  deviceId   String
  fcmToken   String
  apnsToken  String?
  platform   String   @default("ios")
  appVersion String?
  isActive   Boolean  @default(true)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  @@unique([userId, deviceId])
  @@index([userId, isActive])
  @@index([fcmToken])
}

model NotificationSend {
  id             String    @id @default(cuid())
  title          String
  body           String
  imageUrl       String?
  data           String?
  targetType     String
  targetValue    String?
  deepLinkType   String?
  deepLinkId     String?
  scheduledAt    DateTime?
  sentAt         DateTime?
  status         String    @default("draft")
  sentCount      Int       @default(0)
  deliveredCount Int       @default(0)
  openedCount    Int       @default(0)
  failedCount    Int       @default(0)
  createdBy      String    @default("admin")
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt

  @@index([status, scheduledAt])
  @@index([createdAt])
}

model WhatsAppMessage {
  id           String    @id @default(cuid())
  direction    String
  waMessageId  String?   @unique
  phoneNumber  String
  userId       String?
  templateName String?
  body         String?
  mediaUrl     String?
  mediaType    String?
  status       String    @default("sent")
  campaignId   String?
  orderId      String?
  errorCode    String?
  errorMessage String?
  sentAt       DateTime  @default(now())
  deliveredAt  DateTime?
  readAt       DateTime?
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  nextRetryAt  DateTime? @map("next_retry_at")
  retryCount   Int       @default(0) @map("retry_count")
  cartId       String?   @map("cart_id")
  recoveryStage Int?     @map("recovery_stage")
  cart         Cart?     @relation(fields: [cartId], references: [id], onDelete: SetNull)

  @@index([phoneNumber, createdAt])
  @@index([userId])
  @@index([campaignId])
  @@index([status])
  @@index([cartId])
  @@index([cartId, recoveryStage])
  @@unique([cartId, recoveryStage])
  @@map("whatsapp_messages")
}

model WhatsAppCampaign {
  id                String                      @id @default(cuid())
  name              String
  templateName      String
  templateParams    String?
  targetSegment     String                      @default("all")
  scheduledAt       DateTime?
  sentAt            DateTime?
  status            String                      @default("draft")
  createdBy         String                      @default("admin")
  statsSent         Int                         @default(0)
  statsDelivered    Int                         @default(0)
  statsRead         Int                         @default(0)
  statsReplied      Int                         @default(0)
  statsFailed       Int                         @default(0)
  createdAt         DateTime                    @default(now())
  updatedAt         DateTime                    @updatedAt
  campaign_name     String?                     @map("campaign_name")
  click_count       Int                         @default(0) @map("click_count")
  conversions       Int                         @default(0)
  delivered         Int                         @default(0)
  read_count        Int                         @default(0) @map("read_count")
  revenue_generated Float                       @default(0.0) @map("revenue_generated")
  target_audience   String?                     @map("target_audience")
  template_name     String?                     @map("template_name")
  total_sent        Int                         @default(0) @map("total_sent")
  recipients        WhatsAppCampaignRecipient[]

  @@index([status, scheduledAt])
  @@map("whatsapp_campaigns")
}

model WhatsAppSetting {
  key   String @id
  value String

  @@map("whatsapp_settings")
}

model WhatsAppTemplate {
  id         String   @id @default(cuid())
  name       String   @unique
  category   String
  language   String
  status     String
  components Json
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  @@map("whatsapp_templates")
}

model WhatsAppCampaignRecipient {
  id           String           @id @default(cuid())
  campaignId   String
  phone        String
  name         String?
  status       String           @default("queued")
  messageId    String?
  errorMessage String?
  sentAt       DateTime?
  deliveredAt  DateTime?
  readAt       DateTime?
  nextRetryAt  DateTime?        @map("next_retry_at")
  retryCount   Int              @default(0) @map("retry_count")
  campaign     WhatsAppCampaign @relation(fields: [campaignId], references: [id], onDelete: Cascade)

  @@index([campaignId])
  @@index([phone])
  @@map("whatsapp_campaign_recipients")
}

model WhatsAppWebhookEvent {
  id        String   @id @default(cuid())
  eventType String
  payload   Json
  processed Boolean  @default(false)
  createdAt DateTime @default(now())

  @@map("whatsapp_webhook_events")
}

model WhatsAppOptIn {
  id          String   @id @default(cuid())
  phone       String   @unique
  status      String
  consentDate DateTime @default(now())
  source      String

  @@index([phone])
  @@map("whatsapp_opt_ins")
}

model EmailOptIn {
  id          String   @id @default(cuid())
  phone       String   @unique
  email       String?
  status      String
  consentDate DateTime @default(now())
  source      String

  @@index([phone])
  @@map("email_opt_ins")
}

model WhatsAppDeliveryStatus {
  id          String    @id @default(cuid())
  messageId   String    @unique
  phoneNumber String
  status      String
  errorCode   String?
  errorText   String?
  sentAt      DateTime?
  deliveredAt DateTime?
  readAt      DateTime?
  updatedAt   DateTime  @updatedAt

  @@index([phoneNumber])
  @@map("whatsapp_delivery_status")
}

model WhatsAppEvent {
  id            String             @id @default(cuid())
  eventName     String             @map("event_name")
  customerId    String?            @map("customer_id")
  customerPhone String?            @map("customer_phone")
  orderId       String?            @map("order_id")
  productId     String?            @map("product_id")
  eventSource   String             @default("system") @map("event_source")
  metadataJson  String?            @map("metadata_json")
  status        String             @default("pending")
  createdAt     DateTime           @default(now()) @map("created_at")
  logs          WhatsAppEventLog[]

  @@index([eventName])
  @@index([customerPhone])
  @@index([status])
  @@map("whatsapp_events")
}

model WhatsAppEventLog {
  id              String        @id @default(cuid())
  eventId         String        @map("event_id")
  requestPayload  String?       @map("request_payload")
  responsePayload String?       @map("response_payload")
  status          String        @default("success")
  errorMessage    String?       @map("error_message")
  createdAt       DateTime      @default(now()) @map("created_at")
  event           WhatsAppEvent @relation(fields: [eventId], references: [id], onDelete: Cascade)

  @@index([eventId])
  @@map("whatsapp_event_logs")
}

model EmailCampaign {
  id                String    @id @default(cuid())
  name              String
  subject           String
  previewText       String?
  htmlContent       String
  plainText         String?
  fromName          String?   @default("Zica Bella")
  targetSegment     String    @default("all")
  scheduledAt       DateTime?
  sentAt            DateTime?
  status            String    @default("draft")
  createdBy         String    @default("admin")
  statsSent         Int       @default(0)
  statsOpened       Int       @default(0)
  statsClicked      Int       @default(0)
  statsBounced      Int       @default(0)
  statsUnsubscribed Int       @default(0)
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt

  @@index([status, scheduledAt])
}

model SmsCampaign {
  id             String    @id @default(cuid())
  name           String
  messageBody    String
  targetSegment  String    @default("all")
  scheduledAt    DateTime?
  sentAt         DateTime?
  status         String    @default("draft")
  createdBy      String    @default("admin")
  statsSent      Int       @default(0)
  statsDelivered Int       @default(0)
  statsFailed    Int       @default(0)
  dltTemplateId  String?
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt

  @@index([status, scheduledAt])
}

model CampaignAnalyticsEvent {
  id         String   @id @default(cuid())
  channel    String
  campaignId String
  userId     String?
  eventType  String
  metadata   String?
  ipAddress  String?
  userAgent  String?
  createdAt  DateTime @default(now())

  @@index([channel, campaignId, eventType])
  @@index([userId, channel])
  @@index([createdAt])
}

model Discount {
  id              String    @id @default(cuid())
  code            String    @unique
  type            String    @default("percentage")
  value           Float
  minOrderAmount  Float     @default(0)
  maxDiscount     Float?
  startDate       DateTime  @default(now())
  endDate         DateTime?
  usageLimit      Int?
  usageCount      Int       @default(0)
  isActive        Boolean   @default(true)
  description     String?
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
  cashbackEnabled Boolean   @default(false)
  cashbackType    String    @default("percentage")
  cashbackValue   Float     @default(0)
  autoApply       Boolean   @default(false) @map("auto_apply")
  isSecure        Boolean   @default(false) @map("is_secure")

  @@index([code])
  @@index([isActive])
}

model VerificationCode {
  id        String   @id @default(cuid())
  phone     String
  code      String
  expiresAt DateTime
  createdAt DateTime @default(now())

  @@index([phone])
}

model AppLogin {
  id        String   @id @default(cuid())
  phone     String
  status    String   @default("SUCCESS")
  ip        String?
  userAgent String?
  createdAt DateTime @default(now())

  @@index([phone])
  @@index([createdAt])
}

model Policy {
  id        String   @id @default(cuid())
  handle    String   @unique
  title     String
  content   String
  updatedAt DateTime @updatedAt
  createdAt DateTime @default(now())
}

model EmailLog {
  id             String   @id @default(cuid())
  createdAt      DateTime @default(now())
  recipientEmail String
  recipientName  String?
  subject        String
  templateName   String
  triggerEvent   String?
  referenceId    String?
  status         String   @default("sent")
  messageId      String?
  errorMessage   String?
  sentBy         String   @default("system")

  @@index([recipientEmail])
  @@index([createdAt])
  @@index([templateName])
}

model EmailTemplate {
  id                String       @id @default(cuid())
  createdAt         DateTime     @default(now())
  updatedAt         DateTime     @updatedAt
  name              String
  category          String
  subject           String
  htmlBody          String
  variables         Json?
  isActive          Boolean      @default(true)
  createdBy         String?
  automationTrigger String?
  EmailDraft        EmailDraft[]

  @@index([category])
  @@index([automationTrigger])
}

model EmailNotificationPreference {
  id        String   @id @default(cuid())
  eventKey  String   @unique
  label     String
  isEnabled Boolean  @default(true)
  updatedAt DateTime @updatedAt
}

model EmailDraft {
  id           String         @id @default(cuid())
  createdAt    DateTime       @default(now())
  updatedAt    DateTime       @updatedAt
  subject      String?
  toRecipients Json?
  cc           String?
  bcc          String?
  htmlBody     String?
  templateId   String?
  scheduledAt  DateTime?
  createdBy    String?
  template     EmailTemplate? @relation(fields: [templateId], references: [id])

  @@index([templateId])
}

model Wishlist {
  id         String   @id @default(cuid())
  customerId String
  productId  String
  createdAt  DateTime @default(now())
  size       String?
  variantId  String?
  customer   Customer @relation(fields: [customerId], references: [id], onDelete: Cascade)
  product    Product  @relation(fields: [productId], references: [id], onDelete: Cascade)

  @@unique([customerId, productId, variantId])
  @@index([customerId])
  @@index([productId])
}

model Address {
  id         String   @id @default(cuid())
  customerId String
  name       String?
  phone      String?
  phoneLast10 String?
  email      String?
  address1   String
  address2   String?
  city       String
  state      String
  zip        String
  country    String   @default("India")
  isDefault  Boolean  @default(false)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  lat        Float?
  lng        Float?
  placeId    String?  @map("place_id")
  customer   Customer @relation(fields: [customerId], references: [id], onDelete: Cascade)

  @@index([customerId])
  @@index([phoneLast10])
}

model SupportTicket {
  id          String           @id @default(cuid())
  customerId  String?
  guestName   String?
  guestEmail  String?
  subject     String
  status      String           @default("OPEN")
  priority    String           @default("MEDIUM")
  aiAutoReply Boolean          @default(true)
  createdAt   DateTime         @default(now())
  updatedAt   DateTime         @updatedAt
  messages    SupportMessage[]
  customer    Customer?        @relation(fields: [customerId], references: [id])

  @@index([customerId])
  @@index([status, updatedAt])
}

model WhatsAppChatSetting {
  id          String   @id @default(cuid())
  phoneNumber String   @unique
  aiAutoReply Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model SupportMessage {
  id         String        @id @default(cuid())
  ticketId   String
  senderType String        @default("USER")
  senderId   String?
  senderName String?
  content    String
  createdAt  DateTime      @default(now())
  ticket     SupportTicket @relation(fields: [ticketId], references: [id], onDelete: Cascade)
}

model User {
  id                   String                    @id @default(cuid())
  email                String                    @unique
  passwordHash         String
  name                 String?
  role                 Role                      @default(ADMIN)
  isActive             Boolean                   @default(true)
  createdAt            DateTime                  @default(now())
  updatedAt            DateTime                  @updatedAt
  createdBy            String?
  failedLoginAttempts  Int                       @default(0)
  lockUntil            DateTime?
  needsPasswordChange  Boolean                   @default(false)
  auditLogs            AuditLog[]
  permissions          Permission[]
  creator              User?                     @relation("UserCreator", fields: [createdBy], references: [id])
  createdUsers         User[]                    @relation("UserCreator")
  designTasksApproved  MfgDesignTask[]           @relation("DesignApprover")
  designTasksCreated   MfgDesignTask[]           @relation("DesignCreator")
  mfgDesignAssignedBy  MfgDesignAssignment[]     @relation("DesignAssigner")
  mfgDesignAssignments MfgDesignAssignment[]     @relation("DesignAssignee")
  mfgPerformanceEvents MfgEmployeePerformance[]  @relation("EmployeePerf")
  mfgKBDesigns         MfgProductKnowledgeBase[] @relation("KBDesigner")
  mfgSamplesReviewed   MfgSample[]               @relation("SampleReviewer")
  assignedMfgTasks     MfgTask[]                 @relation("TaskAssignee")
}

model Permission {
  id        String  @id @default(cuid())
  userId    String
  module    Module
  canView   Boolean @default(false)
  canEdit   Boolean @default(false)
  canDelete Boolean @default(false)
  pages     String?
  user      User    @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, module])
}

model AuditLog {
  id        String   @id @default(cuid())
  userId    String?
  action    String
  module    String?
  targetId  String?
  metadata  Json?
  ipAddress String?
  userAgent String?
  timestamp DateTime @default(now())
  user      User?    @relation(fields: [userId], references: [id])

  @@index([userId])
  @@index([action])
  @@index([timestamp])
}

model AdminNotificationRead {
  id         String   @id @default(cuid())
  userId     String   @unique
  lastReadAt DateTime @default(now())
  updatedAt  DateTime @updatedAt

  @@index([userId])
}

model AIChatSession {
  id        String          @id @default(cuid())
  userId    String?
  title     String?
  createdAt DateTime        @default(now())
  updatedAt DateTime        @updatedAt
  messages  AIChatMessage[]

  @@index([userId])
}

model AIChatMessage {
  id        String        @id @default(cuid())
  sessionId String
  role      String
  content   String
  createdAt DateTime      @default(now())
  session   AIChatSession @relation(fields: [sessionId], references: [id], onDelete: Cascade)

  @@index([sessionId])
}

model ZicaAiCache {
  id                  String   @id @default(uuid())
  userId              String
  sessionId           String
  turnIndex           Int
  userMessage         String
  aiResponse          String
  detectedProducts    String[]
  detectedCollections String[]
  intentTags          String[]
  sentiment           String
  timestamp           DateTime @default(now())
  responseTokens      Int
  helpfulFlag         Boolean?

  @@index([userId, timestamp])
}

model ZicaAiGlobalInsight {
  id              String   @id @default(uuid())
  insightType     String
  key             String
  frequency       Int      @default(1)
  lastSeen        DateTime @default(now())
  sampleQueries   String[]
  confidenceScore Float    @default(0.0)

  @@unique([insightType, key])
}

model ZicaUserProfile {
  userId              String   @id
  preferredCategories String[]
  preferredSizes      String[]
  styleTags           String[]
  totalSessions       Int      @default(0)
  lastActive          DateTime @default(now())
  favouriteProducts   String[]
}

model AiInteractionLog {
  id             String   @id @default(cuid())
  principalKind  String
  principalId    String?
  channel        String   // app | support | whatsapp | admin
  modelUsed      String
  tokensIn       Int
  tokensOut      Int
  latencyMs      Int
  toolsCalled    String[] @default([])
  guardTripped   Boolean  @default(false)
  fallbackUsed   Boolean  @default(false)
  createdAt      DateTime @default(now())

  @@index([principalKind, createdAt])
  @@index([channel, createdAt])
}

model AiTokenUsage {
  id          String   @id @default(cuid())
  customerId  String
  date        String   // YYYY-MM-DD
  totalTokens Int      @default(0)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@unique([customerId, date])
  @@index([customerId, date])
}

model MobileOrder {
  id                String            @id @default(cuid())
  orderNumber       String            @unique
  shopifyOrderId    String?
  customerId        String
  status            String            @default("pending")
  paymentStatus     String            @default("pending")
  paymentMethod     String?
  paymentId         String?
  totalPrice        Float             @default(0)
  subtotalPrice     Float?
  totalTax          Float?
  discountAmount    Float?            @default(0)
  discountCode      String?
  currency          String            @default("INR")
  fulfillmentStatus String            @default("unfulfilled")
  deliveryStatus    String            @default("pending")
  shippingAddress   String?
  billingAddress    String?
  note              String?
  tags              String?
  source            String            @default("mobile_app")
  createdAt         DateTime          @default(now())
  updatedAt         DateTime          @updatedAt
  syncedAt          DateTime?
  customer          Customer          @relation(fields: [customerId], references: [id])
  items             MobileOrderItem[]

  @@index([customerId])
  @@index([status])
  @@index([createdAt])
}

model MobileOrderItem {
  id            String      @id @default(cuid())
  mobileOrderId String
  productId     String?
  variantId     String?
  title         String
  quantity      Int
  price         Float
  sku           String?
  image         String?
  mobileOrder   MobileOrder @relation(fields: [mobileOrderId], references: [id], onDelete: Cascade)
  product       Product?    @relation(fields: [productId], references: [id])

  @@index([mobileOrderId])
}

model WebStoreOrder {
  id                  String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  orderNumber         String   @unique @map("order_number")
  customerName        String   @map("customer_name")
  customerEmail       String   @map("customer_email")
  customerPhone       String   @map("customer_phone")
  phoneLast10         String?  @map("phone_last_10")
  shippingAddress     Json     @map("shipping_address")
  items               Json     @map("items")
  subtotal            Decimal  @db.Decimal(10, 2)
  shippingCharge      Decimal  @map("shipping_charge") @db.Decimal(10, 2)
  discountCode        String?  @map("discount_code")
  discountAmount      Decimal  @default(0) @map("discount_amount") @db.Decimal(10, 2)
  totalAmount         Decimal  @map("total_amount") @db.Decimal(10, 2)
  paymentStatus       String   @map("payment_status")
  paymentMethod       String   @map("payment_method")
  razorpayOrderId     String?  @map("razorpay_order_id")
  razorpayPaymentId   String?  @map("razorpay_payment_id")
  fulfillmentStatus   String   @default("unfulfilled") @map("fulfillment_status")
  trackingNumber      String?  @map("tracking_number")
  trackingUrl         String?  @map("tracking_url")
  notes               String?
  source              String   @default("web")
  createdAt           DateTime @default(now()) @map("created_at")
  updatedAt           DateTime @default(now()) @updatedAt @map("updated_at")
  codUpfrontPaid      Decimal  @default(0) @map("cod_upfront_paid") @db.Decimal(10, 2)
  codUpfrontPaymentId String?  @map("cod_upfront_payment_id")
  storeCreditAmount   Decimal? @default(0) @map("store_credit_amount") @db.Decimal(10, 2)
  paymentFailureReason String? @map("payment_failure_reason")

  @@index([createdAt])
  @@index([paymentStatus, createdAt])
  @@index([fulfillmentStatus, createdAt])
  @@index([razorpayOrderId])
  @@index([phoneLast10])
  @@map("web_store_orders")
}

model WebStoreCustomer {
  id                  String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  email               String             @unique
  name                String
  phone               String
  phoneLast10         String?            @map("phone_last_10")
  passwordHash        String             @map("password_hash")
  addresses           Json               @default("[]")
  defaultAddressIndex Int                @default(0) @map("default_address_index")
  createdAt           DateTime           @default(now()) @map("created_at")
  updatedAt           DateTime           @default(now()) @updatedAt @map("updated_at")
  wishlists           WebStoreWishlist[]

  @@index([phoneLast10])
  @@map("web_store_customers")
}

model WebStoreWishlist {
  id         String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  customerId String           @map("customer_id") @db.Uuid
  productId  String           @map("product_id")
  variantId  String           @map("variant_id")
  addedAt    DateTime         @default(now()) @map("added_at")
  customer   WebStoreCustomer @relation(fields: [customerId], references: [id], onDelete: Cascade)

  @@map("web_store_wishlists")
}

model WebStoreCoupon {
  id                   String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  code                 String   @unique
  discountType         String   @map("discount_type")
  discountValue        Decimal  @map("discount_value") @db.Decimal(10, 2)
  minOrderValue        Decimal  @default(0) @map("min_order_value") @db.Decimal(10, 2)
  usageLimit           Int?     @map("usage_limit")
  usedCount            Int      @default(0) @map("used_count")
  validFrom            DateTime @map("valid_from")
  validUntil           DateTime @map("valid_until")
  isActive             Boolean  @default(true) @map("is_active")
  createdAt            DateTime @default(now()) @map("created_at")
  applicability        String   @default("ALL") @map("applicability")
  applyAsStoreCredit   Boolean  @default(false) @map("apply_as_store_credit")
  codDiscountType      String   @default("percentage") @map("cod_discount_type")
  codDiscountValue     Decimal  @default(0) @map("cod_discount_value") @db.Decimal(10, 2)
  prepaidDiscountType  String   @default("percentage") @map("prepaid_discount_type")
  prepaidDiscountValue Decimal  @default(0) @map("prepaid_discount_value") @db.Decimal(10, 2)
  cashbackEnabled      Boolean  @default(false) @map("cashback_enabled")
  cashbackType         String   @default("percentage") @map("cashback_type")
  cashbackValue        Decimal  @default(0) @map("cashback_value") @db.Decimal(10, 2)
  autoApply            Boolean  @default(false) @map("auto_apply")
  isSecure             Boolean  @default(false) @map("is_secure")

  @@map("web_store_coupons")
}

model WebStoreBanner {
  id             String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  title          String
  subtitle       String?
  imageUrl       String   @map("image_url")
  mobileImageUrl String?  @map("mobile_image_url")
  ctaLabel       String?  @map("cta_label")
  ctaLink        String?  @map("cta_link")
  position       Int
  isActive       Boolean  @map("is_active")
  createdAt      DateTime @default(now()) @map("created_at")

  @@map("web_store_banners")
}

model WebStoreGalleryImage {
  id        String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  title     String?
  subtitle  String?
  imageUrl  String   @map("image_url")
  altText   String?  @map("alt_text")
  linkUrl   String?  @map("link_url")
  position  Int      @default(0)
  isActive  Boolean  @default(true) @map("is_active")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@map("web_store_gallery_images")
}

model MfgCollection {
  id        String                @id @default(cuid())
  name      String
  season    String?
  status    String                @default("ACTIVE")
  createdAt DateTime              @default(now()) @map("created_at")
  updatedAt DateTime              @updatedAt @map("updated_at")
  designs   MfgDesignAssignment[]

  @@map("mfg_collections")
}

model MfgDesignAssignment {
  id                 String                @id @default(cuid())
  collectionId       String                @map("collection_id")
  styleCode          String                @unique @map("style_code")
  styleName          String                @map("style_name")
  productCategory    String                @map("product_category")
  assignedToId       String?               @map("assigned_to_id")
  assignedById       String?               @map("assigned_by_id")
  assignmentDate     DateTime              @default(now()) @map("assignment_date")
  submissionDeadline DateTime?             @map("submission_deadline")
  priority           String                @default("MEDIUM")
  status             String                @default("Not Started")
  notes              String?
  createdAt          DateTime              @default(now()) @map("created_at")
  updatedAt          DateTime              @updatedAt @map("updated_at")
  assignedBy         User?                 @relation("DesignAssigner", fields: [assignedById], references: [id])
  assignedTo         User?                 @relation("DesignAssignee", fields: [assignedToId], references: [id])
  collection         MfgCollection         @relation(fields: [collectionId], references: [id])
  attachments        MfgDesignAttachment[]
  samples            MfgSample[]

  @@index([collectionId])
  @@index([assignedToId])
  @@map("mfg_design_assignments")
}

model MfgDesignAttachment {
  id           String              @id @default(cuid())
  assignmentId String              @map("assignment_id")
  type         String
  fileUrl      String              @map("file_url")
  fileName     String?             @map("file_name")
  uploadedAt   DateTime            @default(now()) @map("uploaded_at")
  assignment   MfgDesignAssignment @relation(fields: [assignmentId], references: [id], onDelete: Cascade)

  @@map("mfg_design_attachments")
}

model MfgSample {
  id                 String              @id @default(cuid())
  designAssignmentId String              @map("design_assignment_id")
  styleCode          String              @map("style_code")
  productName        String              @map("product_name")
  fabricType         String?             @map("fabric_type")
  gsm                String?
  composition        String?
  printingTechnique  String?             @map("printing_technique")
  embroideryDetails  String?             @map("embroidery_details")
  accessoriesUsed    String?             @map("accessories_used")
  vendorId           String?             @map("vendor_id")
  remarks            String?
  status             String              @default("Pending Review")
  submittedAt        DateTime            @default(now()) @map("submitted_at")
  reviewedAt         DateTime?           @map("reviewed_at")
  reviewedById       String?             @map("reviewed_by_id")
  adminComments      String?             @map("admin_comments")
  images             MfgSampleImage[]
  revisions          MfgSampleRevision[]
  designAssignment   MfgDesignAssignment @relation(fields: [designAssignmentId], references: [id])
  reviewedBy         User?               @relation("SampleReviewer", fields: [reviewedById], references: [id])
  vendor             MfgVendor?          @relation(fields: [vendorId], references: [id])

  @@index([designAssignmentId])
  @@map("mfg_samples")
}

model MfgSampleImage {
  id         String    @id @default(cuid())
  sampleId   String    @map("sample_id")
  imageUrl   String    @map("image_url")
  uploadedAt DateTime  @default(now()) @map("uploaded_at")
  sample     MfgSample @relation(fields: [sampleId], references: [id], onDelete: Cascade)

  @@map("mfg_sample_images")
}

model MfgSampleRevision {
  id               String    @id @default(cuid())
  sampleId         String    @map("sample_id")
  revisionNumber   Int       @map("revision_number")
  changeRequests   String    @map("change_requests")
  designerResponse String?   @map("designer_response")
  resubmissionDate DateTime? @map("resubmission_date")
  approvalDate     DateTime? @map("approval_date")
  createdAt        DateTime  @default(now()) @map("created_at")
  sample           MfgSample @relation(fields: [sampleId], references: [id], onDelete: Cascade)

  @@map("mfg_sample_revisions")
}

model MfgProductKnowledgeBase {
  id                String     @id @default(cuid())
  styleCode         String     @unique @map("style_code")
  productName       String     @map("product_name")
  designerId        String?    @map("designer_id")
  fabricUsed        String?    @map("fabric_used")
  gsm               String?
  composition       String?
  printingTechnique String?    @map("printing_technique")
  vendorId          String?    @map("vendor_id")
  accessoriesUsed   String?    @map("accessories_used")
  sampleImages      String[]   @map("sample_images")
  productionNotes   String?    @map("production_notes")
  approvalDate      DateTime?  @map("approval_date")
  createdAt         DateTime   @default(now()) @map("created_at")
  designer          User?      @relation("KBDesigner", fields: [designerId], references: [id])
  vendor            MfgVendor? @relation("KBVendor", fields: [vendorId], references: [id])

  @@map("mfg_product_knowledge_base")
}

model MfgEmployeePerformance {
  id            String   @id @default(cuid())
  userId        String   @map("user_id")
  eventType     String   @map("event_type")
  scoreDelta    Float    @map("score_delta")
  referenceId   String?  @map("reference_id")
  referenceType String?  @map("reference_type")
  notes         String?
  createdAt     DateTime @default(now()) @map("created_at")
  user          User     @relation("EmployeePerf", fields: [userId], references: [id])

  @@index([userId])
  @@map("mfg_employee_performance")
}

model StoreSettings {
  id              String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  pageKey         String   @unique @map("page_key")
  homePageTitle   String?  @map("home_page_title")
  metaDescription String?  @map("meta_description")
  socialImageUrl  String?  @map("social_image_url")
  socialImageAlt  String?  @map("social_image_alt")
  twitterCardType String   @default("summary_large_image") @map("twitter_card_type")
  updatedBy       String?  @map("updated_by")
  updatedAt       DateTime @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  createdAt       DateTime @default(now()) @map("created_at") @db.Timestamptz(6)

  @@map("store_settings")
}

model SyncLog {
  id        String   @id @default(cuid())
  orderId   String
  action    String
  status    String
  error     String?
  payload   String?
  createdAt DateTime @default(now())
}

model RateLimitLog {
  id        Int       @id @default(autoincrement())
  key       String    @db.VarChar(255)
  timestamp DateTime? @default(now()) @db.Timestamptz(6)

  @@index([key, timestamp], map: "idx_ratelimit_key_timestamp")
}

model price_tag_batches {
  id             String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  batch_number   Int       @default(1)
  product_id     String
  product_name   String
  generic_name   String
  mrp            Decimal   @db.Decimal(10, 2)
  size           String
  quantity       Int       @default(1)
  sku_prefix     String
  tags_generated Json      @default("[]")
  created_at     DateTime? @default(now()) @db.Timestamptz(6)
  updated_at     DateTime? @default(now()) @db.Timestamptz(6)
}

model price_tag_sku_counters {
  id              String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  sku_variant_key String    @unique
  last_counter    Int       @default(0)
  updated_at      DateTime? @default(now()) @db.Timestamptz(6)
}

model product_skus {
  id                 String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  product_id         String
  sku                String    @unique
  size               String
  quantity           Int       @default(1)
  status             String    @default("IN_STOCK")
  created_at         DateTime? @default(now()) @db.Timestamptz(6)
  shopify_variant_id String?
  inventory_item_id  String?
}

enum Role {
  SUPER_ADMIN
  ADMIN
}

enum Module {
  DASHBOARD_HOME
  SUPPORT
  ORDERS
  MOBILE_ORDERS
  CUSTOMERS
  PRODUCTS
  INVENTORY
  LOGISTICS
  RETURNS_EXCHANGES
  STOREFRONT
  COMMUNITY
  MARKETING
  MANUFACTURING
  FINANCIAL
  INTEGRATIONS
  AI_SERVICES
  SETTINGS
  ADMIN_USERS
  AUDIT_LOG
  ANALYTICS
}

model AnalyticsEvent {
  id          String   @id @default(cuid())
  eventId     String   @unique @map("event_id")
  eventName   String   @map("event_name")
  sessionId   String?  @map("session_id")
  customerId  String?  @map("customer_id")
  anonymousId String?  @map("anonymous_id")
  platform    String   @default("web")
  productId   String?  @map("product_id")
  variantId   String?  @map("variant_id")
  cartId      String?  @map("cart_id")
  orderId     String?  @map("order_id")
  value       Float?
  currency    String   @default("INR")
  quantity    Int?
  pageUrl     String?  @map("page_url")
  referrer    String?
  utmSource   String?  @map("utm_source")
  utmMedium   String?  @map("utm_medium")
  utmCampaign String?  @map("utm_campaign")
  utmContent  String?  @map("utm_content")
  utmTerm     String?  @map("utm_term")
  deviceType  String?  @map("device_type")
  browser     String?
  os          String?
  countryCode String?  @map("country_code")
  country     String?
  region      String?
  city        String?
  lat         Float?
  lng         Float?
  metadata    Json?
  createdAt   DateTime @default(now()) @map("created_at")

  @@index([eventName, createdAt])
  @@index([sessionId])
  @@index([customerId])
  @@index([anonymousId])
  @@index([productId])
  @@index([cartId])
  @@index([orderId])
  @@index([createdAt])
  @@index([platform, createdAt])
  @@map("analytics_events")
}

model AnalyticsSession {
  id           String    @id @default(cuid())
  anonymousId  String    @map("anonymous_id")
  customerId   String?   @map("customer_id")
  platform     String    @default("web")
  landingPage  String?   @map("landing_page")
  currentPage  String?   @map("current_page")
  pageViews    Int       @default(0) @map("page_views")
  deviceType   String?   @map("device_type")
  browser      String?
  os           String?
  referrer     String?
  utmSource    String?   @map("utm_source")
  utmMedium    String?   @map("utm_medium")
  utmCampaign  String?   @map("utm_campaign")
  countryCode  String?   @map("country_code")
  country      String?
  region       String?
  city         String?
  lat          Float?
  lng          Float?
  isNew        Boolean   @default(true) @map("is_new")
  startedAt    DateTime  @default(now()) @map("started_at")
  lastActiveAt DateTime  @default(now()) @map("last_active_at")
  endedAt      DateTime? @map("ended_at")
  duration     Int?

  @@index([anonymousId])
  @@index([customerId])
  @@index([startedAt])
  @@index([lastActiveAt])
  @@index([platform, startedAt])
  @@map("analytics_sessions")
}

model WhatsAppSchedulerRun {
  id                           String   @id @default(cuid())
  createdAt                    DateTime @default(now()) @map("created_at")
  campaignsProcessed           Int      @default(0) @map("campaigns_processed")
  strandedRecipientsRecovered  Int      @default(0) @map("stranded_recipients_recovered")
  campaignRecipientsRetried    Int      @default(0) @map("campaign_recipients_retried")
  messagesRetried              Int      @default(0) @map("messages_retried")
  abandonedCartStep1Sent       Int      @default(0) @map("abandoned_cart_step1_sent")
  abandonedCartStep2Sent       Int      @default(0) @map("abandoned_cart_step2_sent")
  abandonedCartStep3Sent       Int      @default(0) @map("abandoned_cart_step3_sent")
  errorCount                   Int      @default(0) @map("error_count")
  errors                       String?  @db.Text
  success                      Boolean  @default(true)

  @@map("whatsapp_scheduler_runs")
}

/// Global Multi-Country Storefront — country/currency configuration
model GlobalStoreCountry {
  id             String   @id @default(cuid())
  code           String   @unique            // ISO 3166-1 alpha-2: IN, US, GB, CA, AU, AE, DE, ES
  name           String                       // "United States"
  currencyCode   String   @map("currency_code")  // ISO 4217: USD, GBP, CAD, AUD, AED, EUR, INR
  currencySymbol String   @map("currency_symbol") // $, £, $, $, د.إ, €, ₹
  locale         String   @default("en-IN")   // BCP 47 locale for Intl.NumberFormat
  isBase         Boolean  @default(false)     // true only for IN — locks multiplier at 1.0
  multiplier     Decimal  @default(1.0) @db.Decimal(10, 4)  // e.g. 2.5 — applied to INR base price
  exchangeRate   Decimal  @db.Decimal(12, 6)  // INR → currencyCode conversion rate, admin-editable
  isActive       Boolean  @default(true)
  sortOrder      Int      @default(0)
  updatedAt      DateTime @updatedAt
  createdAt      DateTime @default(now())

  @@map("global_store_countries")
}

/// Singleton settings row for global store feature toggles
model GlobalStoreSettings {
  id                 String   @id @default("singleton")
  globalStoreEnabled Boolean  @default(false) @map("global_store_enabled")
  updatedAt          DateTime @updatedAt
  createdAt          DateTime @default(now())

  @@map("global_store_settings")
}
